models:
  - name: fct_sales_agg
    description: 'adds total amounts and net_average_price as support PowerBI analyse'
    columns:
      - name: gross_amount
        data_tests:
          - dbt_utils.expression_is_true:
              expression: '>= 0'
  - name: fct_stocks
    description: 'final table for PowerBI'
  
  - name: dim_product
    description: 'table reference for interaction with PowerBI' 

  - name: dim_stores
    description: 'table reference for interaction with PowerBI'

models:

  - name: mrt_operations__daily_order_performance
    description: "This model summarizes daily order data by city and state, including total orders, total order amount, and average order amount."
    columns:
    - name: total_orders  
      tests:
        - dbt_utils.expression_is_true:
            expression: '>= 0'